version: "3.9"

services:
  # ==========================================
  # MySQL Database
  # ==========================================
#  mysql:
#    image: mysql:8.0
#    container_name: aidocqa-mysql
#    restart: unless-stopped
#    environment:
#      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root}
#      MYSQL_DATABASE: ${MYSQL_DATABASE:-aidocqa}
#    ports:
#      - "3307:3306"
#    volumes:
#      - mysql_data:/var/lib/mysql
#    networks:
#      - aidocqa-network
#    healthcheck:
#      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD:-root}"]
#      interval: 10s
#      timeout: 5s
#      retries: 10
#      start_period: 30s

  # ==========================================
  # Spring Boot Backend API
  # ==========================================
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aidocqa-backend
    restart: unless-stopped
    environment:
      MYSQL_HOST: ${MYSQL_HOST:-mysql-333106d6-kriteshjaiswal0007-bef1.e.aivencloud.com}
      MYSQL_PORT: ${MYSQL_PORT:-10428}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-aidocqa}
      MYSQL_USER: ${MYSQL_USER:-avnadmin}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-AVNS_wCnf8i72hXVEAt6iEzw}
      GROQ_API_KEY: ${GROQ_API_KEY:-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}
      GROQ_MODEL: ${GROQ_MODEL:-llama-3.3-70b-versatile}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-AQ.Ab8RN6J1i0GWyunWhkeqqAGl2YpID-SCa3iSU8hQU1eCH_PMTg}
    ports:
      - "8080:8080"
    volumes:
      - uploads_data:/app/uploads
    networks:
      - aidocqa-network

volumes:
  uploads_data:

networks:
  aidocqa-network:
    driver: bridge
